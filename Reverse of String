class Solution {
    public void reverseString(char[] s) {
         int n=s.length;
        char[] c=new char[n];
        for(int i=0;i<n;i++){
            c[i]=s[n-i-1];
        }
        for(int i=0;i<n;i++){
            s[i]=c[i];
            System.out.print(s[i]+",");
        } 
    }
}
